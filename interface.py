import tkinter

class gui:
    def __init__(self, scripts, title = 'Main_Skin_Interface'):
        self.scripts = scripts


scripts = lambda file, files: for file in os.listdir(os.getcwd())

print(scripts)
